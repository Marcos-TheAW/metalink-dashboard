---
import { badgeClasses } from '../lib/badges';

interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<span class={`inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium ${badgeClasses(value)}`}>
  {label}
</span>
